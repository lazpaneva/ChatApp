@model ChatViewModel
<p></p>
<form asp-controller="Chat" asp-action="Send" method="post">
    <div class="form-group card-header row">
        <div class="col-12">
            <h5>Send a new message</h5>
        </div>
        <div class="col-8">
            <label>Message: </label>
            <textarea asp-for="CurrentMessage.MessageText" class="form-control" rows="3"></textarea>
            <span asp-validation-for="CurrentMessage.MessageText"></span>
        </div>
        <div class="col-4">
            <label>Sender name: </label>
            <input asp-for="CurrentMessage.Sender" class="form-control" />
            <input class="btn btn-primary mt-2 float-lg-right" type="submit" value="Send" />
        </div>
    </div>
</form>

@*<h4>Send a new maessage</h4>
<form asp-controller="Chat" asp-action="Send" method="post">
    <div class="form-group row col-2">
        <div>
            <label>Message: </label>
            <textarea class="form-control" asp-for="CurrentMessage.MessageText" rows="3"></textarea>
        </div>
        <div class="form-group row-2 col-1">
            <label>Sender Name:</label>
            <input type="text" class="form-control" asp-for="CurrentMessage.Sender">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
    </div>

</form>*@